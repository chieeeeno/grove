# Grove Roadmap

> Grove の育成戦略・マイルストーン・意思決定ログ
> 最終更新: 2026-04-07

---

## マイルストーン

「何を作るか」は [grove-design.md](./grove-design.md) を参照。
ここでは「誰のために、いつまでに、どこまで」を管理する。

### M0: 自分用 α

**ゴール**: 自分が grove-app の worktree を Grove で管理できる状態。

**完成条件**:
- 自分が毎日使えるクオリティ
- UI は最低限、エラー握りつぶしOK（preflight 警告は ADR-0012 に従う）
- Phase 2（Claude Code 検知）の技術的実現性が立証済み

**スコープ**:

#### 基盤
- [ ] Tauri 2 + React + TypeScript + Tailwind v4 プロジェクトのセットアップ
- [ ] Zustand による状態管理の導入
- [ ] tauri-plugin-store 導入
- [ ] tauri-plugin-window-state 導入（ウィンドウサイズ・位置の保存）
- [ ] UI は日本語のみ（ADR-0009）

#### リポジトリ管理
- [ ] 複数リポジトリ対応（左サイドバー）
- [ ] リポジトリ追加（ディレクトリ選択ダイアログ）
- [ ] リポジトリ削除（登録解除のみ、実ファイルは消さない）
- [ ] リポジトリ切り替え
- [ ] リポジトリ登録の永続化（store）

#### worktree 表示
- [ ] worktree カードの Grid 表示
- [ ] カード表示項目: ラベル / ブランチ名（副次）/ 変更ファイル数（合計のみ、ADR-0011）/ 最終コミット / VS Code ボタン / Remove ボタン
- [ ] main / primary worktree には目印（バッジ等）を付ける、Remove ボタン非表示
- [ ] **作成機能は持たない**（外部の Claude Code スキル等で作成、Grove は管理専用 — M1 で追加予定）

#### worktree ラベル機能（ADR-0008）
- [ ] デフォルト表示は worktree のディレクトリ名
- [ ] 鉛筆アイコン → インライン編集モード → 確定ボタン or Cmd+Enter で確定
- [ ] キャンセルは Esc キー or × アイコン
- [ ] Enter 単独では確定しない
- [ ] ラベルの永続化キーは worktree の絶対パス
- [ ] rename された場合はラベル消失（M1 で rename 機能と同時に対応）

#### worktree 削除
- [ ] Remove ボタン → 確認ダイアログを必ず表示
- [ ] 未コミットの変更がある場合、ダイアログに警告表示
- [ ] 「ブランチも一緒に削除」チェックボックス
- [ ] Yes 確定で worktree 削除（force 含む） + 必要ならブランチ削除

#### エディタ連携
- [ ] VS Code で開くボタン（`code <path>` を実行）
- [ ] 起動時に `code` コマンドの存在を preflight チェック（ADR-0012）
- [ ] `code` 不在時は上部バナー警告 + ボタン無効化 + ホバー時ツールチップ

#### リフレッシュ（ADR-0013）
- [ ] 自動リフレッシュ（時間間隔ポーリング、初期値 5 秒）
- [ ] 手動リフレッシュボタン
- [ ] Cmd+R ショートカット

#### 検証
- [ ] Phase 2（Claude Code 検知）の技術スパイクを完了する（M0 リリース前の必須条件）

**M0 では実装しないもの（M1 以降に回したもの）**:
- worktree 新規作成機能（M1 で追加）
- worktree rename 機能（M1 早期で追加、ADR-0008）
- ahead/behind 表示（ADR-0010）
- ファイル監視ベースのリフレッシュ（M1 早期で追加、ADR-0013）
- 選択中リポジトリの永続化（M1 で追加、優先度高）
- Claude Code エージェント可視化（Phase 2 = M0 完成後）
- Detail Panel
- 詳細パネル系の機能（変更ファイル一覧、コミット履歴、エージェントチーム表示）
- Terminal 起動 / Diff 表示ボタン
- テーマ対応（ADR-0006 のテレメトリ同様、M2 以降）
- i18n / 英語対応（ADR-0009）

---

### M1: 配布可能 β

**ゴール**: 友達・同僚に「使ってみて」と渡せる状態。

**完成条件**:
- README 整備（インストール手順、初回起動時の「右クリック→開く」案内含む）
- エラーハンドリングまとも
- 設定ファイルのスキーマ確定（後方互換考慮開始）
- GitHub Releases で配布（ADR-0007）

**スコープ**（M0 から先送りしたもの中心、優先度順）:

#### 優先度: 高（M1 早期で対応）
- [ ] **ファイル監視ベースのリフレッシュ**（`notify` crate、ADR-0013）
  - ポーリングからの移行、デバウンス・監視解除・FSEvents 罠回避を含む
- [ ] **worktree rename 機能**（ADR-0008）
  - カードメニューに「Rename worktree」追加
  - `git worktree move` 経由で実行、ラベルキーも同時更新
- [ ] **選択中リポジトリの永続化**
  - 起動時に最後に選択していたリポジトリを復元

#### 優先度: 中（M1 リリース前に必須）
- [ ] **worktree 新規作成機能**
  - 設計書では Phase 1 に含まれていたが、開発者本人が外部スキルで作成する運用のため M0 から外していた
  - 配布先のユーザーは Claude Code スキルを持っていないため M1 では必須
  - 仕様詳細は M1 着手時に詰める（既存ブランチ / 新規ブランチ、base 指定、dir 名指定 等）
- [ ] **エラーハンドリングの整備**
  - M0 で握りつぶしていたエラーをユーザーに伝える
  - ADR-0012 の preflight 原則に従う

#### 優先度: 低〜中（必要性を見て判断）
- [ ] **ahead/behind 表示**（ADR-0010）
  - fetch タイミング・キャッシュ戦略を含めた設計が必要
- [ ] **詳細パネル（Detail Panel）の最小実装**
  - 変更ファイル一覧
  - 直近のコミット履歴
  - エージェント情報は Phase 2 待ち
- [ ] **Cmd+R 以外のキーボードショートカット**
  - リポジトリ切り替え、worktree 選択など
- [ ] **テーマ対応の検討**
  - ライト / ダーク / システム連動
  - β 配布で「ダーク欲しい」が出てから判断

#### Phase 2（Claude Code 連携）の M1 段階での扱い
- [ ] **Claude Code プロセス検知**（基本機能のみ）
  - worktree ごとの「稼働中 / 待機中」表示
  - 詳細なエージェントチーム情報は ADR-0008 / スパイク結果に基づき野心を下方修正
- [ ] **コミット帰属の手動運用ガイド**
  - 自動判定は不可と判明済み（スパイク結果）
  - コミットメッセージ規約での運用案を README に記載

#### 配布作業
- [ ] LICENSE ファイル設置（Apache-2.0、ADR-0003）
- [ ] README 整備
- [ ] GitHub Releases での配布フロー構築
- [ ] dmg ビルドの自動化（GitHub Actions 等）
- [ ] 「右クリック→開く」手順の README 記載（ADR-0007）

**M1 では実装しないもの（M2 以降に持ち越し）**:
- コード署名 + 公証（ADR-0007、M2 移行時に再判断）
- Homebrew tap での配布（ADR-0007）
- Windows / Linux 対応（ADR-0004）
- i18n / 英語対応（ADR-0009）
- テレメトリ（ADR-0006）
- 自動更新機能

---

### M2: 公開 v1.0

**ゴール**: 不特定多数に公開。Hacker News / Reddit / X 等で告知できる完成度。

**完成条件**:
- 自動更新が動く
- README / ドキュメントサイトが整っている
- 1 年以上メンテを続ける覚悟が固まっている

**スコープ**:

#### 配布・運用（M2 の中核）
- [ ] **自動更新機能**
  - tauri-plugin-updater の導入
  - GitHub Releases と連動
- [ ] **Homebrew tap での配布**（ADR-0007）
  - `brew install --cask chieeeeno/tap/grove` で導入可能に
  - tap リポジトリの管理 + Cask 定義の保守
- [ ] **GitHub Actions でのリリースフロー自動化**
  - dmg ビルド → 署名 → 公証 → Releases 公開 → Homebrew tap 更新まで一気通貫

#### ドキュメント
- [ ] **ドキュメントサイト構築**
  - インストール手順、機能説明、トラブルシューティング
  - VitePress / Docusaurus 等を検討
- [ ] **README の本格整備**
  - スクリーンショット、特徴、競合比較
- [ ] **CONTRIBUTING.md / CODE_OF_CONDUCT.md** など OSS 標準ファイル

#### 機能拡張（M2 で再判断するもの）
- [ ] **i18n / 英語対応**（ADR-0009）
  - 海外ユーザーの反応を見て判断
  - react-i18next を第一候補に
- [ ] **テレメトリ（opt-in）**（ADR-0006）
  - ユーザー数が増えてフィードバックの直接収集が困難になったら検討
  - opt-out は採用しない
- [ ] **Windows / Linux 対応の判断**（ADR-0004）
  - 要望が出たら判断
  - 対応する場合は実機検証環境の確保が前提
- [ ] **Phase 3（Polish）系の機能**
  - エディタ選択（Cursor 等）
  - Terminal 起動ボタン
  - Diff 表示
  - 通知機能（エージェント完了時等）
  - テーマ対応（ライト / ダーク / システム）
  - 設定画面
  - キーボードショートカットの拡充

#### コミュニティ・成長
- [ ] **GitHub Issues 運用開始**
  - バグ報告 / 機能要望のテンプレート整備
  - ROADMAP の管理を Issues / Projects に移すか判断
- [ ] **告知**
  - Hacker News / Reddit r/git / r/macapps / X での告知
  - 個人ブログでの紹介記事

**M2 で実装するか未定のもの**:
- **Apple Developer Program 登録 + コード署名 + 公証**（ADR-0007）
  - 年 $99 のサブスクリプションを継続するかが論点
  - M2 段階でも判断保留。ユーザー数・要望・継続意志を見て判断する
  - 採用しない場合は M2 でも「右クリック→開く」運用を継続
- 公式 Homebrew core への登録（審査必要、需要次第）
- Cursor 連携
- Detail Panel のフル実装（エージェントチーム可視化等）
- カスタマイズ可能な UI（カラム配置、表示項目選択等）

**M2 ステータスの判断基準**:
M1 を完成させた後、以下が満たされたら M2 着手を判断する:
- M1 を 1 ヶ月以上自分で使い続け、致命的な問題がない
- 知人配布で複数のフィードバックを得ている
- 「Grove を継続的に育てる」意志が固まっている
- 年 $99 を継続して払う判断ができる

---

## 捨てない決断（Locked-in Decisions）

後から変えると痛いやつ。先に決めて固定する。

| 項目 | 決定 | 決定日 | メモ |
|---|---|---|---|
| アプリ名 | Grove | 2026-04-07 | 確定 |
| アプリ識別子 | `io.github.chieeeeno.grove` | 2026-04-07 | GitHub アカウント名前空間 |
| ライセンス | Apache-2.0 | 2026-04-07 | Rust エコシステムと作法を揃える + 特許保護 |
| 設定ファイルパス | tauri-plugin-store デフォルト | 2026-04-07 | `~/Library/Application Support/io.github.chieeeeno.grove/`。詳細は ADR-0005 |
| 設定スキーマバージョニング | TBD | - | migration 戦略 |
| 配布形態（M1） | GitHub Releases のみ・署名なし | 2026-04-07 | M2 移行時に署名を再検討。詳細は ADR-0007 |
| 対応OS（M0/M1） | macOS のみ | 2026-04-07 | 開発者が macOS のみ所有。詳細は ADR-0004 |
| テレメトリ | 実装しない（将来 opt-in 追加余地あり） | 2026-04-07 | 詳細は ADR-0006 |

---

## 意思決定ログ（Decision Log）

各決定の背景・選択肢・理由を記録する。後から「なんでこうしたんやっけ？」を辿れるようにするため。
形式は ADR（Architecture Decision Record）の簡易版。

---

### ADR-001: アプリ名を「Grove」で確定
**日付**: 2026-04-07
**ステータス**: 確定

**背景**:
設計書では「Grove（仮）」表記。リポジトリ名・アプリ識別子・配布チャネル名（Homebrew tap 等）に影響するため、早期に確定したい。命名候補と選定経緯は [grove-naming.md](./grove-naming.md) を参照。

**検討した選択肢**:
- A: Grove で確定
- B: 別の名前（Canopy, Arbor, Copse, Mori, Graft 等の候補あり）
- C: 仮のまま進める

**決定**: A（Grove で確定）

**理由**:
- 「木立、小さな森」の意味が、worktree が並ぶ様子と自然に重なる
- 短くて覚えやすい（5文字）
- worktree の「tree」とモチーフがつながる
- CLI コマンド名としても打ちやすい（`grove`）
- npm / crates.io での名前の被りが少ない
- 技術的すぎず親しみやすい響き
- 仮のまま進めると識別子など他の決定がブロックされる

**却下した選択肢の理由**:
- B: 詳細は [grove-naming.md](./grove-naming.md) の候補一覧参照。Grove より優位な候補が見当たらなかった
- C: 意思決定の先送りであり、他の決定（識別子、リポジトリ名）が進められない

---

### ADR-002: アプリ識別子を `io.github.chieeeeno.grove` に決定
**日付**: 2026-04-07
**ステータス**: 確定

**背景**:
Tauri の `tauri.conf.json` で必須。逆ドメイン形式が慣習。後から変更するとアップデート時に別アプリ扱いになるため、早期確定が必要。

**検討した選択肢**:
- A: `com.tomoki.grove` — シンプル
- B: `dev.tomoki.grove` — 開発者ツール系の流派
- C: `io.github.chieeeeno.grove` — GitHub アカウント名前空間
- D: 独自ドメイン

**決定**: C（`io.github.chieeeeno.grove`）

**理由**:
- 「自分が所有する名前空間を使う」というベストプラクティスに従う
- GitHub アカウント `chieeeeno` は実質的に自分の所有する一意な名前空間
- `com.tomoki.*` は誰でも名乗れるため厳密には正しくない
- 独自ドメインを持っていない現状でのベストな選択

**却下した選択肢の理由**:
- A/B: 所有していない名前空間を使うのは識別子の本来の意図に反する
- D: ドメインを持っていないため不可

---

### ADR-003: ライセンスを Apache-2.0 に決定
**日付**: 2026-04-07
**ステータス**: 確定

**背景**:
配布を見据えるなら早期確定が必要。後から変更するには全コントリビューターの同意が必要になり、人数が増えるほど困難になる。自分一人のうちに決めるのが最もコスト低い。

**検討した選択肢**:
- A: MIT — シンプル、開発者ツールの定番
- B: Apache-2.0 — Rust エコシステム標準、特許保護あり
- C: GPL-3.0 — コピーレフト、フォーク商用化を防げる
- D: 未定

**決定**: B（Apache-2.0）

**理由**:
1. **Rust エコシステムとの作法統一** — Tauri、git2、sysinfo、tokio など Grove が依存する主要クレートは全て Apache-2.0 OR MIT のデュアルライセンス。Grove も Apache-2.0 にすることで Rust コミュニティの慣習と揃い、後から見た人が違和感を持たない
2. **特許保護がタダで付く** — コントリビューターが持つ特許の自動許諾条項があり、企業ユーザーが安心して採用できる。個人ツールの段階では実害ゼロだが、将来 Grove が伸びた時の保険になる
3. **手間は MIT と実質同じ** — ライセンス文は公式定型文をコピペするだけ。NOTICE ファイルも個人プロジェクト初期は不要

**却下した選択肢の理由**:
- A（MIT）: 技術的には問題ないが、Rust エコシステム標準から外れる + 特許保護がない。Apache-2.0 を選ばない積極的理由がない
- C（GPL-3.0）: コピーレフトの伝染性により Grove のコードを他プロジェクトに取り込みにくくなる。Rust エコシステム内で浮く。「フォーク商用化を絶対防ぎたい」という強い意志がない限り過剰
- D（未定）: 配布フェーズに入る前のどこかで必ず決める必要があり、先送りする意味がない

---

## 意思決定ログ

個別の意思決定（ADR）は [docs/adr/](./docs/adr/) を参照。

---

## 検証ログ（Spike Results）

技術的な未知を潰した記録。「やってみて分かったこと」を残す。

### Claude Code 検知の実現可能性
**ステータス**: 二次調査完了
**最終更新**: 2026-04-07
**目的**: Phase 2 の前提が成立するか確認

#### 確定した事実

**1. `~/.claude/projects/` の命名規約が判明**
- worktree の絶対パスの `/` を `-` に置換した名前で projects 配下にディレクトリが作られる
- 例: `/Users/tomoki/work/private/grove-app` → `~/.claude/projects/-Users-tomoki-work-private-grove-app/`
- **これにより Grove は worktree のパスから対応する Claude セッションデータの場所を計算で求められる**

**2. セッションログの形式**
- `~/.claude/projects/<encoded-path>/<session-uuid>.jsonl` に保存される
- JSONL 形式（1 行 1 イベント）
- リアルタイムに追記されるため tail で監視可能
- ファイル名 = セッション UUID（複数セッションが同一プロジェクト下に並ぶ）
- 同じディレクトリに `<session-uuid>/` サブフォルダもある（補助データ）

**3. 稼働中の claude プロセス検知**
- `ps aux | grep claude` で claude プロセスを列挙可能
- メインプロセスとサブエージェントは引数で区別できる
  - メインプロセス: シンプルな `claude` 起動
  - サブエージェント: `claude --output-format stream-json --verbose --input-format stream-json --model ... --disallowedTools ... --permission-mode default`
- `lsof -p <pid> | grep cwd` でメインプロセスの cwd（= worktree のパス）が取れる

**4. worktree とプロセスの紐付け**
- メインプロセスの cwd が worktree の絶対パスと一致する
- Grove は worktree のパス一覧を持っているため、cwd と照合するだけで「どの worktree で claude が動いているか」を判定可能

#### 未確定 / 制約あり

**1. サブエージェントの cwd 取得**
- 二次調査時点で、サブエージェント（PID 別）の `lsof | grep cwd` は出力なし
- 親プロセスの cwd と同じはずなので、メインプロセス経由で十分判定可能と推定
- 実装時に再検証が必要

**2. stream-json のスキーマ**
- 公式ドキュメントが取得できておらず、ログ内のイベント構造の詳細は未確認
- ただし JSONL 形式が確定したため、実機で1イベントずつパースして仕様を逆算可能
- Agent SDK が発行するイベント種別、Lead/Sub の関係表現方法は実装段階で確認する

**3. エージェントチーム（Lead/Sub）の詳細復元**
- スキーマ非公開のため依然として確証なし
- ただし JSONL ファイルは取得可能で、サブエージェント起動引数（`stream-json` モード）も判明したため、実装時にイベントを観察すれば一定の復元は可能と推定

#### 結論

**✅ 実現可能（一部修正前提）**

一次調査時点の「⚠️ 一部修正が必要」から **「✅ 実現可能（一部修正前提）」** に格上げ。

Phase 2 の核となる「worktree ごとの Claude Code 稼働検知」「セッションログの読み取り」は実現可能で、実装方針も具体化できた。エージェントチームの詳細可視化は依然として実装時の検証次第だが、根拠となるデータ（JSONL ログ）は確実にアクセスできる。

#### 実装指針（Phase 2 着手時の参考）

1. **稼働検知**: `ps aux | grep claude` + `lsof -p <pid> | grep cwd` を Tauri command でラップ
2. **セッションデータ参照**: worktree path から `~/.claude/projects/<encoded>/` を計算してアクセス
3. **リアルタイム更新**: `<session-uuid>.jsonl` を tail（`notify` crate でファイル監視）
4. **エージェント詳細**: 実装初期にスパイクして JSONL イベントの種別を仕様化、設計書を更新

#### スパイクで使った調査コマンド（再現用）

```bash
# claude プロセス一覧
ps aux | grep -i claude | grep -v grep

# プロセスの cwd 取得
lsof -p <pid> | grep cwd

# プロジェクトディレクトリ確認
ls ~/.claude/projects/

# セッションログ確認
ls -la ~/.claude/projects/<encoded-path>/
```

---

## 変更履歴
- 2026-04-07: 初版作成（マイルストーン・捨てない決断・検証ログの枠のみ）
